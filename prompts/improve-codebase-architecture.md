---
description: Find architecture deepening opportunities in the current codebase
argument-hint: "[focus area]"
---
Read `~/.pi/agent/skills/improve-codebase-architecture/SKILL.md` and follow it for this architecture review.

When the skill references supporting files, resolve them relative to `~/.pi/agent/skills/improve-codebase-architecture/` and read them before using their guidance.

Use pi/Solo equivalents for any other agent-harness-specific instructions: explore with `read`, `bash`/`rg`, and/or a Solo `scout` subagent when useful. Lean on Solo primitives: keep findings and decisions in an architecture review scratchpad, use subagent scratchpads for scouts/interface alternatives, and create Solo todos for confirmed implementation work instead of ad hoc task lists.

Focus area or request:
$ARGUMENTS
